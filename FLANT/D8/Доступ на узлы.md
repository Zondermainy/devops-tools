Система -> Управление системой -> Доступ -> Права доступа: продвинутые настройки -> Доступ в узлах

Создание:
- Имя, указать UID
- В зависимости от требований, можно:
 1) дать sudo,
  2) добавить пользователя сразу на тачки группы узлов,
   3) добавить его в группу линукс,
    4) накинуть хеш пароля (создать запись в /etc/shadow),
     5) добавить ssh ключ

пользователей добавляет скрипт /var/lib/bashible/bundle_steps/000_add_node_users.sh

пользователи лежат в /home/deckhouse/

Проверить:
'''yaml
kubectl get nodeusers.deckhouse.io deckhouseadmin
'''

Удалить: 
'''yaml
kubectl delete nodeusers.deckhouse.io deckhouseadmin
'''